{% extends 'base.html.twig' %}

{% block title %}My Profile - eco. online store {% endblock %}

{% block content %}
    <h2>My Profile</h2>

    {% if app.user %}
{#        <div class="mb-3">#}
{#            You are logged in as {{ app.user.firstname }}#}
{#        </div>#}
        <hr>

        <div class="row">
            <div class="col-md-4">
                <div class="profile-item text-center">
                    <img class="mb-2" src="{{ asset('assets/img/password.png') }}" alt="password"><br>
                    <a href="{{ path('password') }}">My Password</a>
                </div>
            </div>
            <div class="col-md-4">
                <div class="profile-item text-center">
                    <img class="mb-2" src="{{ asset('assets/img/address.png') }}" alt="address"><br>
                    <a href="{{ path('address') }}">My Addresses</a>
                </div>
            </div>
            <div class="col-md-4">
                <div class="profile-item text-center">
                    <img class="mb-2" src="{{ asset('assets/img/orders.png') }}" alt="orders"><br>
                    <a href="{{ path('my_orders') }}">My Orders</a>
                </div>
            </div>
        </div>
    {% endif %}
{% endblock %}
