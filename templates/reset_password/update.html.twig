{% extends 'base.html.twig' %}

{% block title %}Reset Password - eco. online store {% endblock %}

{% block content %}
    {% for message in app.flashes('notice') %}
        <div class="alert alert-danger">{{ message }}</div>
    {% endfor %}
    <h2>Reset Password</h2>
    <hr>

    {{ form(form) }}
{% endblock %}